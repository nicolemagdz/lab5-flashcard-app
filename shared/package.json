{
  "name": "@flashcard/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript contracts (entities, DTOs, API envelope types) consumed by both frontend and backend.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
